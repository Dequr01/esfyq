/**
 * Custom hook for magnetic scroll snapping
 * Disabled per user request.
 */
export function useMagneticScroll() {
  // Disabled magnetic scrolling: hook performs no actions
  // Returns empty cleanup function
  return () => {}
}
